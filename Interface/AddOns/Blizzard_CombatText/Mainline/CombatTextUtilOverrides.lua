function CombatTextUtil.GetComboPointsMessageInfo(_messageType, _data, _displayType)
	-- Mainline combat text doesn't have special handling for UNIT_POWER_UPDATE
	-- events for COMBO_POINTS and opts to not display them as special messages.
end
